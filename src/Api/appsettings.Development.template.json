{
  "_comment": "Copy this file to appsettings.Development.json (gitignored) and fill in a real local password, or use `dotnet user-secrets set ConnectionStrings:Default ...` instead.",
  "ConnectionStrings": {
    "Default": "Host=localhost;Database=bccsafety;Username=bcc_app;Password=<set-locally-from-the-secret-store>;Multiplexing=false"
  }
}
